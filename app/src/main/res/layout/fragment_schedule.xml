<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

        <data>

        </data>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/schedule_fragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            tools:context=".app.ui.ScheduleFragment"
            tools:viewBindingIgnore="false">
                >
                <com.alamkanak.weekview.WeekView
                    android:id="@+id/weekView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:defaultEventColor="@color/purple_500"
                    app:eventTextColor="@color/white"
                    app:timeColumnTextSize="12sp"
                    app:hourHeight="50sp"
                    app:timeColumnPadding="0dp"
                    app:timeColumnTextColor="#6E6E6E"
                    app:timeColumnBackgroundColor="@color/white"
                    app:headerPadding="12dp"
                    app:columnGap="0dp"
                    app:numberOfVisibleDays="7"
                    app:headerBackgroundColor="@color/white"
                    app:dayBackgroundColor="@color/white"
                    app:todayBackgroundColor="#BABABA"
                    app:todayHeaderTextColor="#6E6E6E"
                    app:eventPaddingHorizontal="12dp"
                    app:eventPaddingVertical="12dp"
                    app:eventMarginVertical="12dp"
                    app:eventCornerRadius="8dp"
                    />

                <com.google.android.material.floatingactionbutton.FloatingActionButton
                    android:id="@+id/add_event"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="24dp"
                    android:clickable="true"
                    app:backgroundTint="@color/orange_500"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintHorizontal_bias="0.954"
                    app:layout_constraintStart_toStartOf="parent"
                    app:rippleColor="#FFFFFF"
                    app:srcCompat="@drawable/add_ic" />

        </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
